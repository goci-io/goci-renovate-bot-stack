{
    "gitAuthor": "Renovate Bot <apps+renovate@goci.io>",
    "platform": "${git_type}",
    "endpoint": "${git_endpoint}",
    "privateKeyPath": "/usr/src/app/tls/decrypt.key",
    "trustLevel": "low",
    "hostRules": [
        {
            "baseUrl": "${git_endpoint}",
            "encrypted": {
                "token": "${git_encrypted_token}"
            }
        }
    ],
    "repositories": [${repositories_csv}]
}
